import HomeClientExperiment from "@/components/HomeClientExperiment";

export const metadata = {
  title: "Experiment | Portfolio",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ExperimentPage() {
  return <HomeClientExperiment />;
}
